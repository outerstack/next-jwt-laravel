// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['ui-avatars.com'], // Add any other image domains you need
    },
  }
  
  export default nextConfig;
  
